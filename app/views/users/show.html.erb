<h1><%= "#{@user.first_name} #{@user.last_name}" %></h1>


<h2><%= "#{@user.first_name}'s Projects:" %></h2>
<ul>
	<% @user.owned_projects.each do |project| %>
		<li>
			<strong><%= link_to project.title, project %></strong><br>
		</li>
  <% end %>
</ul>
