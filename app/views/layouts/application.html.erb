<!DOCTYPE html>
<html>
  <head>
    <title>Crowdfunder</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">

    <%= stylesheet_link_tag    "https://fonts.googleapis.com/css?family=Open+Sans:700|Roboto+Slab" %>
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <%= csrf_meta_tags %>
  </head>

  <body>
    <header class="site-header">
      <h1 class="logo"><%= link_to 'Crowdfunder', root_path %></h1>

      <nav class="site-nav">
        <% if current_user %>
          <%= link_to "Logout", user_sessions_path, method: :delete %>
        <% else %>
          <%= link_to "Register", new_user_path %> |
          <%= link_to "Login", :login %>
        <% end %>
      </nav>
    </header>

    <% flash.each do |key, message| %>
      <div class="flash-<%= key %>"><%= message %></div>
    <% end %>

    <main>
      <%= yield %>
    </main>

    <footer class="site-footer">
      <a href="https://github.com/sizzlecentral"><div class="footer-list">
			<h3>Don Cooper</h3>
			<p>sizzlecentral</p>
		</div></a>
    <a href="https://github.com/emanuelsamel"><div class="footer-list">
			<h3>Emanuel Samel</h3>
			<p>emanuelsamel</p>
		</div></a>
    <br>
      <small>&copy; 2017 / A Bitmaker assignment</small>
    </footer>
  </body>
</html>
